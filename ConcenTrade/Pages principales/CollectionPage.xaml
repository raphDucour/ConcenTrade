<Page x:Class="Concentrade.Pages_principales.CollectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Concentrade.Pages_principales"
      xmlns:collection="clr-namespace:Concentrade.Pages_principales.Collection"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="CollectionPage" Loaded="Page_Loaded">

    <Grid Background="{StaticResource ZenBackgroundBrush}">
        <!-- Fond sombre et Canvas pour les particules animées -->
        <Canvas x:Name="ParticleCanvas" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>   <!-- Titre et retour -->
                <RowDefinition Height="*"/>      <!-- ScrollViewer -->
                <RowDefinition Height="Auto"/>   <!-- Bouton -->
            </Grid.RowDefinitions>

            <!-- Bouton Retour en haut à gauche -->
            <Button x:Name="RetourButton" Content="&lt; Retour" Style="{StaticResource ZenButtonStyle}"
                    MaxWidth="120" Height="40" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,20,10,10"
                    Click="RetourButton_Click" Grid.Row="0"/>

            <TextBlock Text="Ma Collection" Style="{StaticResource PageTitle}" Grid.Row="0" Margin="0,40,0,20" HorizontalAlignment="Center"/>

            <!-- Panel des cartes avec une barre de défilement -->
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Margin="20,0,20,20">
                <WrapPanel x:Name="CardsPanel" HorizontalAlignment="Center"/>
            </ScrollViewer>

            <!-- Bouton pour acheter des caisses -->
            <Button x:Name="AcheterButton" Content="Ouvrir une Caisse" Style="{StaticResource ZenButtonStyle}"
                    Grid.Row="2" HorizontalAlignment="Center" Margin="0,0,0,20"
                    MaxWidth="350" Height="50" Padding="0,20" FontSize="22" FontWeight="Bold"
                    Click="AcheterButton_Click"/>
        </Grid>

        <!-- Overlay pour l'achat, maintenant plus stylisé -->
        <Grid x:Name="AchatOverlay" Background="#DD000000" Visibility="Collapsed">
            <Border Background="#2B3A4A" CornerRadius="20" Padding="20"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    BorderBrush="{StaticResource ZenButtonBorderBrush}" BorderThickness="1"
                    MaxWidth="800" MinWidth="250">
                <StackPanel>
                    <TextBlock Text="Boutique des Caisses" Style="{StaticResource PageTitle}" FontSize="28" Margin="0,0,0,20" TextAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <!-- Caisse 1 -->
                        <Button Click="BuyCat_Click" Style="{StaticResource ZenButtonStyle}" Margin="10" Padding="10" MaxWidth="200">
                            <StackPanel>
                                <TextBlock Text="Caisse du Débutant" FontWeight="Bold" Foreground="White" TextAlignment="Center"/>
                                <TextBlock Text="Contient les animaux de base" TextWrapping="Wrap" Foreground="#CCCCCC" TextAlignment="Center" Margin="0,5,0,0"/>
                                <TextBlock Text="Coût: 100 Points" FontStyle="Italic" Foreground="#AAAAAA" TextAlignment="Center" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Button>
                        <!-- Caisse 2 -->
                        <Grid>
                            <Button x:Name="Caisse2Button" Click="BuyDog_Click" Style="{StaticResource ZenButtonStyle}" Margin="10" Padding="10" MaxWidth="200">
                                <StackPanel>
                                    <TextBlock Text="Caisse de l'Explorateur" FontWeight="Bold" Foreground="White" TextAlignment="Center"/>
                                    <TextBlock Text="Débloque des compagnons rares" TextWrapping="Wrap" Foreground="#CCCCCC" TextAlignment="Center" Margin="0,5,0,0"/>
                                    <TextBlock x:Name="Caisse2Cost" Text="Coût: 300 Points" FontStyle="Italic" Foreground="#AAAAAA" TextAlignment="Center" Margin="0,10,0,0"/>
                                </StackPanel>
                            </Button>
                            <TextBlock x:Name="Caisse2Lock" Text="🔒" FontSize="36" Foreground="#FFD700" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                        </Grid>
                        <!-- Caisse 3 -->
                        <Grid>
                            <Button x:Name="Caisse3Button" Click="BuyDragon_Click" Style="{StaticResource ZenButtonStyle}" Margin="10" Padding="10" MaxWidth="200">
                                <StackPanel>
                                    <TextBlock Text="Caisse du Maître Zen" FontWeight="Bold" Foreground="White" TextAlignment="Center"/>
                                    <TextBlock Text="Des créatures épiques vous attendent" TextWrapping="Wrap" Foreground="#CCCCCC" TextAlignment="Center" Margin="0,5,0,0"/>
                                    <TextBlock x:Name="Caisse3Cost" Text="Coût: 800 Points" FontStyle="Italic" Foreground="#AAAAAA" TextAlignment="Center" Margin="0,10,0,0"/>
                                </StackPanel>
                            </Button>
                            <TextBlock x:Name="Caisse3Lock" Text="🔒" FontSize="36" Foreground="#FFD700" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                        </Grid>
                    </StackPanel>
                    <Button Content="Fermer" Style="{StaticResource ZenButtonStyle}" Click="CloseAchatOverlay_Click"
                            Margin="0,20,0,0" MaxWidth="150" Height="40" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
