<Page x:Class="ConcenTrade.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="SettingsPage"
      Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="CouleurFondPrincipale" Color="#2B3A4A"/>
        <SolidColorBrush x:Key="CouleurTextePrincipale" Color="#EAEAEA"/>
        <SolidColorBrush x:Key="CouleurBordure" Color="#FFFFFF" Opacity="0.5"/>
        <SolidColorBrush x:Key="CouleurFondTextBox" Color="#454A5C"/>
        <SolidColorBrush x:Key="CouleurSurvolBouton" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="CouleurSurvolItem" Color="#FFFFFF" Opacity="0.1"/>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource CouleurTextePrincipale}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Opacity" Value="0.8"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CouleurFondTextBox}"/>
            <Setter Property="Foreground" Value="{StaticResource CouleurTextePrincipale}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CouleurBordure}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Opacity" Value="0.6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Opacity="{TemplateBinding Opacity}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource CouleurTextePrincipale}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border"
                                Padding="{TemplateBinding Padding}"
                                Background="Transparent">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource CouleurSurvolItem}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource CouleurFondTextBox}"/>
            <Setter Property="Foreground" Value="{StaticResource CouleurTextePrincipale}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CouleurBordure}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton
                                x:Name="ToggleButton"
                                Focusable="false"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" 
                                                Background="{StaticResource CouleurFondTextBox}"
                                                BorderBrush="{StaticResource CouleurBordure}"
                                                BorderThickness="1"
                                                CornerRadius="5"
                                                Opacity="0.6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="30" />
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                                  Content="{TemplateBinding Content}"
                                                                  Margin="{TemplateBinding Padding}"
                                                                  HorizontalAlignment="Left"
                                                                  VerticalAlignment="Center" />
                                                <Path Grid.Column="1"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{StaticResource CouleurTextePrincipale}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Opacity" Value="1"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="Opacity" Value="1"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left" />

                            <Popup
                                Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{StaticResource CouleurFondTextBox}"
                                            BorderThickness="1"
                                            BorderBrush="{StaticResource CouleurBordure}"
                                            CornerRadius="5"/>
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutlineButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource CouleurTextePrincipale}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CouleurBordure}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource CouleurSurvolBouton}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource CouleurSurvolBouton}"/>
                                <Setter Property="Foreground" Value="{StaticResource CouleurFondPrincipale}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource CouleurFondPrincipale}">
        <Canvas x:Name="ParticleCanvas"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="500" Margin="20">

                <TextBlock Text="Paramètres" FontSize="48" FontWeight="Bold" Foreground="{StaticResource CouleurTextePrincipale}" Margin="0,0,0,40" HorizontalAlignment="Center"/>

                <TextBlock Text="Informations personnelles" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource CouleurTextePrincipale}" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Prénom" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="PrenomBox" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Date de naissance" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="DateNaissanceBox"
                                 PreviewTextInput="DateNaissance_PreviewTextInput"
                                 TextChanged="DateNaissance_TextChanged"/>
                        <TextBlock x:Name="AgeActuelBlock" Text="Âge" Foreground="{StaticResource CouleurTextePrincipale}" Opacity="0.5" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Préférences" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource CouleurTextePrincipale}" Margin="0,40,0,20" HorizontalAlignment="Center"/>

                <TextBlock Text="À quel moment de la journée es-tu le plus productif ?" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="MomentCombo" Margin="0,0,0,20">
                    <ComboBoxItem Content="Matin"/>
                    <ComboBoxItem Content="Après-midi"/>
                    <ComboBoxItem Content="Soir"/>
                    <ComboBoxItem Content="Nuit"/>
                </ComboBox>

                <TextBlock Text="Te considères-tu comme facilement distrait ?" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="DistraitCombo" Margin="0,0,0,20">
                    <ComboBoxItem Content="Oui"/>
                    <ComboBoxItem Content="Non"/>
                    <ComboBoxItem Content="Un petit peu"/>
                </ComboBox>

                <Button Content="Gérer les applications bloquées" 
                        Style="{StaticResource OutlineButtonStyle}" 
                        HorizontalAlignment="Stretch"
                        Margin="0,10,0,0"
                        Click="GererAppsBloquees_Click"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,0">
                    <Button Content="Retour" Style="{StaticResource OutlineButtonStyle}" Click="Retour_Click" Margin="0,0,10,0" Width="150"/>
                    <Button Content="Sauvegarder" Style="{StaticResource OutlineButtonStyle}" Click="Sauvegarder_Click" Margin="10,0,0,0" Width="150"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>