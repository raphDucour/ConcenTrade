<Page x:Class="Concentrade.Pages_principales.SessionHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d"
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="Statistiques">
    <Page.Resources>
        <!-- Style du graphique à barres (carré) -->
        <Style x:Key="ZenBarChartStyle" TargetType="lvc:CartesianChart">
            <Setter Property="Background" Value="{StaticResource ZenBackgroundBrush}"/>
            <Setter Property="LegendLocation" Value="None"/>
            <Setter Property="Margin" Value="40,0,40,20"/>
        </Style>
        <Style x:Key="ZenLineChartStyle" TargetType="lvc:CartesianChart">
            <Setter Property="Background" Value="#223344"/>
            <Setter Property="LegendLocation" Value="None"/>
            <Setter Property="Margin" Value="0,0,0,30"/>
            <Setter Property="Height" Value="320"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="0" Color="#FFD700" Opacity="0.18"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ZenAxisStyle" TargetType="lvc:Axis">
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Separator" Value="{x:Null}"/>
        </Style>
        <Style x:Key="ZenScrollBarStyle" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border x:Name="PART_Track" Background="#FFFFFF" Opacity="0.3" CornerRadius="6"/>
                            <Track x:Name="PART_TrackControl" Orientation="{TemplateBinding Orientation}" 
                                   Maximum="{TemplateBinding Maximum}" Minimum="{TemplateBinding Minimum}" Value="{TemplateBinding Value}">
                                <Track.Thumb>
                                    <Thumb Height="32" Width="32">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#000000" Opacity="0.8" BorderBrush="#FFFFFF" BorderThickness="2" CornerRadius="16"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{x:Null}" IsTabStop="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{x:Null}" IsTabStop="False"/>
                                </Track.DecreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid Background="{StaticResource ZenBackgroundBrush}">
        <Canvas x:Name="ParticleCanvas" />
        <Button x:Name="RetourButton" Content="&lt; Retour" Style="{StaticResource ZenButtonStyle}"
                Width="100" Height="40" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="40,40,0,0"
                Click="RetourButton_Click"/>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border MaxWidth="1400" MinWidth="900" Padding="40" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Historique de vos sessions" Style="{StaticResource PageTitle}" FontSize="54" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                    <TextBlock Text="Retrouvez ici le détail de vos heures de concentration jour par jour." Foreground="#FFD700" FontSize="22" Margin="0,0,0,40" HorizontalAlignment="Center"/>
                    <!-- Boutons fixes pour le choix Graphique/Tableau -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30" Width="Auto" x:Name="SwitchButtonsPanel">
                        <Button x:Name="BtnGraphique" Content="Graphique" Style="{StaticResource ZenButtonStyle}" Width="220" Height="54" FontSize="22" FontWeight="Bold" Margin="12,0" Click="BtnGraphique_Click"/>
                        <Button x:Name="BtnTableau" Content="Tableau" Style="{StaticResource ZenButtonStyle}" Width="220" Height="54" FontSize="22" FontWeight="Bold" Margin="12,0" Click="BtnTableau_Click"/>
                    </StackPanel>
                    <!-- Contenu dynamique -->
                    <Grid x:Name="ContentSwitcher">
                        <Grid x:Name="GraphiquePanel">
                            <lvc:CartesianChart x:Name="LineChart" Style="{StaticResource ZenLineChartStyle}" Height="600" Margin="0,20,0,20">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Style="{StaticResource ZenAxisStyle}" Title="Date"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Style="{StaticResource ZenAxisStyle}" Title="Heures de concentration"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Grid>
                        <Grid x:Name="TableauPanel" Visibility="Collapsed">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20" Width="Auto">
                                    <Border Background="#37474F" CornerRadius="12" Padding="24,12" Margin="12,0" BorderBrush="#FFD700" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="Moyenne" Foreground="#FFD700" FontWeight="Bold" FontSize="22" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="StatMoyenne" Foreground="White" FontSize="22" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#37474F" CornerRadius="12" Padding="24,12" Margin="12,0" BorderBrush="#FFD700" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="Maximum" Foreground="#FFD700" FontWeight="Bold" FontSize="22" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="StatMax" Foreground="White" FontSize="22" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#37474F" CornerRadius="12" Padding="24,12" Margin="12,0" BorderBrush="#FFD700" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock Text="Total" Foreground="#FFD700" FontWeight="Bold" FontSize="22" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="StatTotal" Foreground="White" FontSize="22" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                                <Border CornerRadius="18" Background="#263445" BorderBrush="#FFD700" BorderThickness="2" Padding="40" Effect="{DynamicResource DropShadowEffect}" Margin="0,0,0,0" Width="1200" Height="600">
                                    <DataGrid x:Name="StatsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                                              RowBackground="#37474F" AlternatingRowBackground="#223344" Foreground="{StaticResource ZenTextBrush}"
                                              Background="Transparent" BorderThickness="0" FontSize="22" GridLinesVisibility="All"
                                              HorizontalAlignment="Stretch" VerticalAlignment="Top" MinHeight="520">
                                        <DataGrid.ColumnHeaderStyle>
                                            <Style TargetType="DataGridColumnHeader">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Setter Property="FontWeight" Value="Bold"/>
                                                <Setter Property="FontSize" Value="22"/>
                                                <Setter Property="Background" Value="#FFD700"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                            </Style>
                                        </DataGrid.ColumnHeaderStyle>
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"/>
                                            <DataGridTextColumn Header="Heures de concentration" Binding="{Binding Hours}" Width="*"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page> 
