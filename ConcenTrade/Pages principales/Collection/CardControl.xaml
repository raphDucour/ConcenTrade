<UserControl x:Class="Concentrade.Pages_principales.Collection.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="320" Width="220">

    <Canvas x:Name="MainGrid">

        <Border x:Name="CardBorder" 
                Width="200" 
                Height="280" 
                CornerRadius="12" 
                Padding="4">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.6" BlurRadius="8"/>
            </Border.Effect>

            <Border CornerRadius="8" BorderThickness="2">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                        <GradientStop Color="#B38728" Offset="0.0" />
                        <GradientStop Color="#FCF6BA" Offset="0.5" />
                        <GradientStop Color="#B38728" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#1D2A3A" Offset="0.0" />
                        <GradientStop Color="#101821" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="CardNameText" Grid.Row="0" HorizontalAlignment="Center" Margin="10,6,10,6" Foreground="White" FontSize="16" FontWeight="Bold"/>

                    <Border Grid.Row="1" Margin="8,0,8,8" CornerRadius="3" Background="#000">
                        <Image x:Name="CardIcon" Stretch="UniformToFill" Margin="1"/>
                    </Border>

                    <StackPanel Grid.Row="2" Margin="12,0,12,12">
                        <TextBlock Text="TIPS" FontWeight="Bold" Foreground="#D4AF37" FontSize="10" Margin="0,0,0,2"/>
                        <Border Height="37" VerticalAlignment="Top" Background="Transparent">
                            <TextBlock x:Name="CardDescriptionText" Text="" TextWrapping="Wrap" Foreground="White" FontSize="11" Margin="0,0,0,5" VerticalAlignment="Top"/>
                        </Border>
                        <Separator Background="#D4AF37" Opacity="0.3" Margin="0,2,0,4"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Canvas>
</UserControl>