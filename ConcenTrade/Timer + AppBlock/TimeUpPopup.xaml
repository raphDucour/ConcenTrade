<Window x:Class="Concentrade.TimeUpPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Temps écoulé" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" SizeToContent="Height">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1">
        <StackPanel Margin="25">
            <TextBlock Text="Dernier avertissement" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="L'application sera fermée après ce délai." TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,15"/>

            <ComboBox x:Name="DurationComboBox" Margin="0,0,0,15" Foreground="White" Background="#2D2D30" BorderBrush="#555"/>

            <Button x:Name="ExtendButton" Content="Prolonger (dernière chance)" Background="#D97706" Click="ExtendButton_Click"/>
            <Button x:Name="CloseButton" Content="Fermer maintenant" Background="#374151" Margin="0,10,0,0" Click="CloseButton_Click"/>
        </StackPanel>
    </Border>
</Window>